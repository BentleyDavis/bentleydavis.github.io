{
  "name": "bentleydavis",
  "version": "1.0.0",
  "description": "Bentley Davis",
  "main": "index.js",
  "scripts": {
    "start": "del dist /q && eleventy --serve --watch",
    "build": "del dist /q && eleventy",
    "debug": "@powershell -Command $env:DEBUG='*'; eleventy",
    "deploy": "del dist /q && eleventy && gh-pages -d dist -b master",
    "upgrade-all": "ncu -u --timeout 60000 && npm install"
  },
  "author": "Bentley Davis <Bentley@BentleyDavis.com> (http://BentleyDavis.com)",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "gh-pages": "^2.2.0",
    "npm-check-updates": "^4.1.2"
  }
}
